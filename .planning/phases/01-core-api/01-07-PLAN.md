---
phase: 01-core-api
plan: 07
type: execute
wave: 2
depends_on: [01-02-PLAN.md, 01-03-PLAN.md]
files_modified: [tests/unit/slug.test.ts, tests/unit/link.test.ts, vitest.config.ts]
autonomous: true
requirements: [F1.2, F1.3]
user_setup: []

must_haves:
  truths:
    - "Slug generation produces valid codes"
    - "Custom slug validation works"
    - "All core functions have tests"
  artifacts:
    - path: "tests/unit/slug.test.ts"
      provides: "Slug utility tests"
      min_lines: 30
    - path: "tests/unit/link.test.ts"
      provides: "Link service tests"
      min_lines: 40
  key_links:
    - from: "tests/unit/slug.test.ts"
      to: "src/utils/slug.ts"
      via: "import"
      pattern: "import.*slug"
---

<objective>
Write unit tests for core functionality
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-api/01-02-PLAN.md

## Key Requirements
- F1.2: Generate unique short codes (6-8 characters)
- F1.3: Custom short code support
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up test framework</name>
  <files>vitest.config.ts, package.json</files>
  <action>
    Install vitest:
    npm install -D vitest @vitest/coverage-v8
    Create vitest.config.ts with:
    - test environment: node
    - coverage provider: v8
    - test match: **/*.test.ts
    Add test scripts to package.json:
    - "test": "vitest"
    - "test:run": "vitest run"
    - "test:cover": "vitest run --coverage"
  </action>
  <verify>
    <automated>npm run test:run</automated>
  </verify>
  <done>Vitest configured and runs</done>
</task>

<task type="auto">
  <name>Task 2: Write slug tests</name>
  <files>tests/unit/slug.test.ts</files>
  <action>
    Create tests/unit/slug.test.ts:
    - generateSlug: returns correct length (6, 7, 8)
    - generateSlug: returns alphanumeric only
    - generateSlug: no confusing characters (0, O, I, l)
    - isValidSlug: accepts valid slugs
    - isValidSlug: rejects invalid characters
    - isValidSlug: rejects reserved words
  </action>
  <verify>
    <automated>npm run test:run -- tests/unit/slug.test.ts</automated>
  </verify>
  <done>All slug tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Write link service tests</name>
  <files>tests/unit/link.test.ts</files>
  <action>
    Create tests/unit/link.test.ts:
    - createLink: creates link with valid URL
    - createLink: rejects invalid URL
    - createLink: uses custom slug when provided
    - createLink: rejects duplicate custom slug
    - getLinkBySlug: returns link for valid slug
    - getLinkBySlug: returns null for invalid slug
    - isValidUrl: validates URL correctly
    Mock Prisma client for testing.
  </action>
  <verify>
    <automated>npm run test:run -- tests/unit/link.test.ts</automated>
  </verify>
  <done>All link service tests pass</done>
</task>

</tasks>

<verification>
- All unit tests passing
- Test coverage > 70% for core modules
- No test flakiness
</verification>

<success_criteria>
- [x] npm run test:run passes
- [x] Slug tests cover all cases
- [x] Link service tests cover main functions
- [x] No console errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-api/01-07-SUMMARY.md`
</output>