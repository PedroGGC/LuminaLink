---
phase: 01-core-api
plan: 03
type: execute
wave: 2
depends_on: [01-02-PLAN.md]
files_modified: [src/routes/redirect.ts, src/app.ts]
autonomous: true
requirements: [F2.1, F2.2, F2.3]
user_setup: []

must_haves:
  truths:
    - "GET /:shortCode redirects to original URL (302)"
    - "Invalid shortCode returns 404"
    - "Click tracking records redirect"
  artifacts:
    - path: "src/routes/redirect.ts"
      provides: "Redirect endpoint"
      min_lines: 40
  key_links:
    - from: "src/routes/redirect.ts"
      to: "src/services/link.service.ts"
      via: "getLinkBySlug"
      pattern: "getLinkBySlug.*slug"
---

<objective>
Implement redirect endpoint :shortCode -> original URL with click tracking
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-api/01-02-PLAN.md

## Key Requirements
- F2.1: Redirect short URL to original URL
- F2.2: Track click statistics on redirect
- F2.3: Handle invalid/expired URLs gracefully
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create redirect route</name>
  <files>src/routes/redirect.ts, src/app.ts</files>
  <action>
    Create src/routes/redirect.ts:
    GET /:shortCode:
    - Look up link in database
    - If not found: return 404 with error JSON { error: "Link not found" }
    - If found but expired (expiresAt < now): return 410 { error: "Link expired" }
    - If found but maxClicks reached: return 410 { error: "Link clicks exhausted" }
    - If found and valid:
        - Use LinkService to increment clickCount
        - Return 302 redirect to originalUrl
        - Set X-Click-Count header for debugging

    Register catch-all route after API routes in app.ts.
  </action>
  <verify>
    <automated>curl -v http://localhost:3000/abc123 -L 2>&1 | grep -E "^< (HTTP|Location)"</automated>
  </verify>
  <done>Valid shortCode redirects (302), invalid returns 404</done>
</task>

</tasks>

<verification>
- GET /validSlug returns 302 redirect to originalUrl
- GET /invalidSlug returns 404
- ClickCount increments on each redirect
</verification>

<success_criteria>
- [x] Valid short code redirects with 302
- [x] Invalid short code returns 404
- [x] Expired links return 410
- [x] Click count increments on redirect
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-api/01-03-SUMMARY.md`
</output>