---
phase: 01-core-api
plan: 05
type: execute
wave: 2
depends_on: [01-02-PLAN.md]
files_modified: [src/routes/preview.ts, src/public/preview.html, src/app.ts]
autonomous: true
requirements: [F2.4]
user_setup: []

must_haves:
  truths:
    - "GET /:shortCode/preview shows interstitial page"
    - "Page displays 'Você está sendo redirecionado para...' message"
    - "Page auto-redirects after delay"
  artifacts:
    - path: "src/routes/preview.ts"
      provides: "Preview endpoint"
      min_lines: 30
    - path: "src/public/preview.html"
      provides: "Interstitial page template"
      min_lines: 30
  key_links:
    - from: "src/routes/preview.ts"
      to: "src/services/link.service.ts"
      via: "getLinkBySlug"
      pattern: "getLinkBySlug.*slug"
---

<objective>
Implement interstitial/preview page for external link warnings
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-api/01-02-PLAN.md

## Key Requirements
- F2.4: Optional interstitial page ("Você está sendo redirecionado para...")
</context>

<tasks>

<task type="auto">
<name>Task 1: Create preview route</name>
<files>src/routes/preview.ts, src/app.ts</files>
<action>
Create src/routes/preview.ts:
GET /:shortCode/preview:
- Look up link by shortCode
- If not found: return 404
- Render preview.html template with:
  - originalUrl
  - domain (extracted from URL)
  - showPreview flag from link
- Set Cache-Control: no-store (prevent caching)

Add fastify-static for serving public files.
</action>
<verify>
<automated>curl -s http://localhost:3000/abc123/preview | grep -o "redirect"</automated>
</verify>
<done>Preview page renders with message</done>
</task>

<task type="auto">
<name>Task 2: Create preview HTML</name>
<files>src/public/preview.html</files>
<action>
Create src/public/preview.html:
- Clean, minimal design
- Warning message: "Você está sendo redirecionado para [domain]..."
- Countdown timer: "Redirecionando em X segundos..."
- "Clique aqui para ir agora" link
- Auto-redirect after countdown (meta refresh or JS)
- Brazilian Portuguese localization
- Mobile-responsive
</action>
<verify>
<automated>cat src/public/preview.html | wc -l</automated>
</verify>
<done>HTML file exists with countdown logic</done>
</task>

</tasks>

<verification>
- Preview page shows warning message
- Countdown redirects after delay
- Manual redirect link works
- Mobile-friendly design
</verification>

<success_criteria>
- [x] GET /:shortCode/preview shows interstitial page
- [x] Page displays target domain
- [x] Auto-redirect works after countdown
- [x] Manual redirect link works
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-api/01-05-SUMMARY.md`
</output>