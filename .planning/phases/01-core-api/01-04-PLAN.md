---
phase: 01-core-api
plan: 04
type: execute
wave: 2
depends_on: [01-02-PLAN.md]
files_modified: [src/routes/qrcode.ts, src/app.ts]
autonomous: true
requirements: [F1.5]
user_setup: []

must_haves:
  truths:
    - "GET /api/links/:shortCode/qr returns QR code image"
    - "QR code encodes the full short URL"
  artifacts:
    - path: "src/routes/qrcode.ts"
      provides: "QR code generation endpoint"
      min_lines: 30
  key_links:
    - from: "src/routes/qrcode.ts"
      to: "qr library"
      via: "create QR code"
      pattern: "QRCode|qrcode"
---

<objective>
Implement QR code generation for short URLs
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-api/01-02-PLAN.md

## Key Requirements
- F1.5: QR Code generation for short URLs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add QR code library</name>
  <files>package.json</files>
  <action>
    Install qrcode library:
    npm install qrcode
    npm install -D @types/qrcode
  </action>
  <verify>
    <automated>npm install --silent && npm list qrcode</automated>
  </verify>
  <done>qrcode installed in node_modules</done>
</task>

<task type="auto">
  <name>Task 2: Create QR code endpoint</name>
  <files>src/routes/qrcode.ts, src/app.ts</files>
  <action>
    Create src/routes/qrcode.ts:
    GET /api/links/:shortCode/qr:
    - Look up link by shortCode
    - If not found: return 404
    - Generate QR code using qrcode.toDataURL(shortUrl, { width: 200, margin: 2 })
    - Return image/png with base64 data or data URL
    - Set Content-Type: image/png
    - Cache-Control: public, max-age=86400 (1 day)
  </action>
  <verify>
    <automated>curl -s http://localhost:3000/api/links/abc123/qr | head -c 20</automated>
  </verify>
  <done>QR code image returned for valid link</done>
</task>

</tasks>

<verification>
- QR code generated for valid short code
- Invalid short code returns 404
- Image is valid PNG format
</verification>

<success_criteria>
- [x] GET /api/links/:shortCode/qr returns PNG image
- [x] QR code encodes correct short URL
- [x] Invalid slug returns 404
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-api/01-04-SUMMARY.md`
</output>