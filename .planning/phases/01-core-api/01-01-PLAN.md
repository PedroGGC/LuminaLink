---
phase: 01-core-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, docker-compose.yml, prisma/schema.prisma, src/app.ts]
autonomous: true
requirements: [F1.1, F1.2]
user_setup: []

must_haves:
  truths:
    - "Node.js project runs with TypeScript"
    - "Fastify server starts on port 3000"
    - "Prisma connects to PostgreSQL"
    - "Link model exists in database"
    - "Docker Compose runs all services"
  artifacts:
    - path: "package.json"
      provides: "Node.js project with dependencies"
      min_lines: 20
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
      min_lines: 15
    - path: "docker-compose.yml"
      provides: "Container orchestration"
      min_lines: 15
    - path: "prisma/schema.prisma"
      provides: "Link database model"
      contains: "model Link"
      min_lines: 30
    - path: "src/app.ts"
      provides: "Fastify server entry point"
      min_lines: 30
  key_links:
    - from: "src/app.ts"
      to: "Fastify server"
      via: "import and listen"
      pattern: "fastify.*listen"
---

<objective>
Initialize the project foundation: Node.js, TypeScript, Fastify, Prisma, Docker
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md

## Key Requirements
- F1.1: Create short URL from long URL
- F1.2: Generate unique short codes (6-8 characters)
- F1.3: Custom short code support
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Node.js project</name>
  <files>package.json, tsconfig.json</files>
  <action>
    Initialize Node.js project with npm init.
    Install dependencies:
    - fastify, @fastify/cors, @fastify/helmet
    - typescript, @types/node, ts-node, nodemon
    - prisma, @prisma/client
    - dotenv
    - zod (for validation in future phases)
    Set up tsconfig.json for strict mode.
    Set up npm scripts: "dev": "nodemon src/app.ts", "build": "tsc", "start": "node dist/app.js"
  </action>
  <verify>
    <automated>npm install --silent && npm run build</automated>
  </verify>
  <done>package.json with all dependencies, tsconfig.json configured</done>
</task>

<task type="auto">
  <name>Task 2: Create Docker Compose</name>
  <files>docker-compose.yml</files>
  <action>
    Create docker-compose.yml with:
    - Node.js service (port 3000:3000)
    - PostgreSQL service (port 5432:5432)
        - POSTGRES_USER: shorten
        - POSTGRES_PASSWORD: shorten
        - POSTGRES_DB: shorten
    - Shared .env file for database URL
    Use official node:20-alpine and postgres:15 images.
    Add healthcheck for both services.
  </action>
  <verify>
    <automated>docker compose config --quiet</automated>
  </verify>
  <done>Docker Compose validates and starts both services</done>
</task>

<task type="auto">
  <name>Task 3: Create Prisma schema</name>
  <files>prisma/schema.prisma, .env</files>
  <action>
    Initialize Prisma: npx prisma init
    Create Link model in schema.prisma:
    - id: UUID (primary key)
    - shortCode: String (unique, indexed)
    - originalUrl: String (not null)
    - customCode: Boolean (default false)
    - hasPassword: Boolean (default false)
    - passwordHash: String? (nullable)
    - expiresAt: DateTime? (nullable)
    - maxClicks: Int? (nullable)
    - clickCount: Int (default 0)
    - showPreview: Boolean (default false)
    - isActive: Boolean (default true)
    - createdAt: DateTime (default now)
    - updatedAt: DateTime (updatedAt)
    Set DATABASE_URL in .env file.
  </action>
  <verify>
    <automated>npx prisma generate && npx prisma db push</automated>
  </verify>
  <done>Prisma client generated, database schema created</done>
</task>

<task type="auto">
  <name>Task 4: Create Fastify server</name>
  <files>src/app.ts</files>
  <action>
    Create src/app.ts with:
    - Fastify instance with CORS and Helmet
    - JSON body parser
    - Health check endpoint (GET /health returning {status: "ok"})
    - Request logging
    - Error handler
    - Graceful shutdown
    Listen on port 3000.
  </action>
  <verify>
    <automated>npm run dev &
sleep 3 && curl -s http://localhost:3000/health</automated>
  </verify>
  <done>Server starts, health check returns {"status":"ok"}</done>
</task>

</tasks>

<verification>
Prisma schema creates Link table with all required fields.
Server starts successfully.
Docker Compose runs without errors.
</verification>

<success_criteria>
- [x] npm run dev starts server without errors
- [x] GET /health returns {status: "ok"}
- [x] npx prisma db push creates Link table
- [x] docker compose up -d runs both services
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-api/01-01-SUMMARY.md`
</output>