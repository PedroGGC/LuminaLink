---
phase: 01-core-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/utils/slug.ts, src/services/link.service.ts, src/routes/links.ts, src/app.ts]
autonomous: true
requirements: [F1.1, F1.2, F1.3, F1.4]
user_setup: []

must_haves:
  truths:
    - "POST /api/links creates short URL and returns it"
    - "GET /api/links/:shortCode returns link info"
    - "Unique short code generation works (6-8 chars)"
    - "Custom short codes accepted"
  artifacts:
    - path: "src/utils/slug.ts"
      provides: "Short code generator utility"
      min_lines: 20
    - path: "src/services/link.service.ts"
      provides: "Link business logic"
      min_lines: 40
    - path: "src/routes/links.ts"
      provides: "Link API routes"
      min_lines: 50
    - path: "src/app.ts"
      provides: "Routes registered in server"
      contains: "routes"
  key_links:
    - from: "src/routes/links.ts"
      to: "src/services/link.service.ts"
      via: "import and call"
      pattern: "import.*link.service"
    - from: "src/services/link.service.ts"
      to: "prisma/client"
      via: "prisma.link.create"
      pattern: "prisma\\.link\\.(create|find)"
---

<objective>
Implement URL shortening core: POST /api/links and GET /api/links/:shortCode endpoints
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-api/01-01-PLAN.md
@.planning/REQUIREMENTS.md

## Key Requirements
- F1.1: Create short URL from long URL
- F1.2: Generate unique short codes (6-8 characters)
- F1.3: Custom short code support (user-defined slugs)
- F1.4: Return short URL in response
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create short code generator</name>
  <files>src/utils/slug.ts</files>
  <action>
    Create src/utils/slug.ts:
    - generateSlug(length: number = 6-8): generates random alphanumeric string
    - isValidSlug(slug: string): validates alphanumeric + hyphens only, no reserved words
    - isSlugAvailable(slug: string): checks database for availability
    Use crypto.randomBytes for secure random generation.
    Characters: a-z, A-Z, 0-9 (omit confusing chars: 0, O, I, l)
  </action>
  <verify>
    <automated>node -e "require('./src/utils/slug').generateSlug()"</automated>
  </verify>
  <done>generateSlug() returns valid 6-8 char alphanumeric string</done>
</task>

<task type="auto">
  <name>Task 2: Create link service</name>
  <files>src/services/link.service.ts</files>
  <action>
    Create src/services/link.service.ts:
    - createLink(originalUrl: string, customSlug?: string, hasPassword?: boolean)
    - getLinkBySlug(slug: string)
    - generateAndStoreUrl(): generates slug, creates link, returns full short URL
    - getBaseUrl(): returns the base URL for short links (configurable)
    Handle duplicate slug errors (409 Conflict).
    Handle invalid URLs (validate with URL constructor).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Link service exported with all CRUD methods</done>
</task>

<task type="auto">
  <name>Task 3: Create link routes</name>
  <files>src/routes/links.ts</files>
  <action>
    Create src/routes/links.ts:
    POST /api/links:
    - Body: { originalUrl: string, customSlug?: string }
    - Validate URL with URL constructor
    - Generate or use custom slug
    - Returns 201: { shortCode, shortUrl, originalUrl, createdAt }
    - Returns 400 for invalid URL, 409 for duplicate slug

    GET /api/links/:shortCode:
    - Returns link info: { shortCode, shortUrl, originalUrl, clickCount, createdAt }
    - Returns 404 if not found

    Register routes in src/app.ts.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/links -H "Content-Type: application/json" -d '{"originalUrl":"https://example.com"}'</automated>
  </verify>
  <done>POST creates short URL, GET retrieves link info</done>
</task>

</tasks>

<verification>
- POST /api/links returns short code and shortUrl
- GET /api/links/:shortCode returns link details
- Custom slugs accepted and validated
- Duplicate slugs rejected with 409
</verification>

<success_criteria>
- [x] POST /api/links with valid URL returns 201 with short code
- [x] POST /api/links with invalid URL returns 400
- [x] POST /api/links with duplicate custom slug returns 409
- [x] GET /api/links/:shortCode returns link info
- [x] GET /api/links/invalid returns 404
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-api/01-02-SUMMARY.md`
</output>