---
phase: 01-core-api
plan: 06
type: execute
wave: 2
depends_on: [01-02-PLAN.md]
files_modified: [src/utils/utm.ts, src/routes/utm.ts]
autonomous: true
requirements: [F1.6]
user_setup: []

must_haves:
  truths:
    - "POST /api/links/utm builds full URL with UTM parameters"
    - "UTM params appended to original URL correctly"
  artifacts:
    - path: "src/utils/utm.ts"
      provides: "UTM parameter builder"
      min_lines: 25
    - path: "src/routes/utm.ts"
      provides: "UTM API endpoint"
      min_lines: 30
  key_links:
    - from: "src/routes/utm.ts"
      to: "src/utils/utm.ts"
      via: "buildUtmUrl"
      pattern: "buildUtmUrl"
---

<objective>
Implement UTM parameter builder for campaign tracking
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-api/01-02-PLAN.md

## Key Requirements
- F1.6: UTM parameter builder (source, medium, campaign)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UTM utility</name>
  <files>src/utils/utm.ts</files>
  <action>
    Create src/utils/utm.ts:
    buildUtmUrl(originalUrl: string, utmParams: {
      source: string,      // required: google, facebook, newsletter
      medium: string,      // required: cpc, email, social
      campaign: string,   // required: spring_sale, product_launch
      term?: string,     // optional: keyword
      content?: string    // optional: banner_variation
    }): string
    - Validates required fields (source, medium, campaign)
    - Appends UTM params to URL using URLSearchParams
    - Handles existing query params correctly (uses &)
    - Returns full URL string
  </action>
  <verify>
    <automated>node -e "const {buildUtmUrl}=require('./src/utils/utm'); console.log(buildUtmUrl('https://ex.com',{source:'google',medium:'cpc',campaign:'test'}))"</automated>
  </verify>
  <done>UTM URL built with correct params</done>
</task>

<task type="auto">
  <name>Task 2: Create UTM endpoint</name>
  <files>src/routes/utm.ts</files>
  <action>
    Create src/routes/utm.ts:
    POST /api/links/utm:
    - Body: { originalUrl, source, medium, campaign, term?, content? }
    - Validates request (Zod schema)
    - Calls buildUtmUrl()
    - Optionally creates short link for UTM URL
    - Returns: { originalUrl, utmUrl, shortCode?, shortUrl? }
    - Validates UTM params meet best practices:
      - source/medium/campaign must be 2+ chars
      - source/medium in allowed list or allow custom
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/links/utm -H "Content-Type: application/json" -d '{"originalUrl":"https://ex.com","source":"google","medium":"cpc","campaign":"test"}'</automated>
  </verify>
  <done>UTM endpoint builds valid URLs</done>
</task>

</tasks>

<verification>
- UTM parameters correctly appended to URL
- Required fields validated
- Optional fields handled
- Duplicate query params handled
</verification>

<success_criteria>
- [x] POST /api/links/utm with valid params returns full URL
- [x] POST /api/links/utm with missing required fields returns 400
- [x] UTM URL has correct format (?utm_source=...)
- [x] Existing query params preserved
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-api/01-06-SUMMARY.md`
</output>